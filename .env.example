# Copy to .env and fill in. NEVER commit .env.
DJANGO_SETTINGS_MODULE=config.settings.local
DJANGO_SECRET_KEY=generate-a-new-one-see-README
DJANGO_DEBUG=True

# MySQL 8.4 LTS — local development server on port 3308
DB_NAME=continuing_education
DB_USER=ce_app
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3308
