{
  "name": "continuing-education-system",
  "private": true,
  "description": "Tailwind build for the Continuing Education Center system (build-time only, not a runtime dependency).",
  "scripts": {
    "build:css": "tailwindcss -i ./static/src/input.css -o ./static/css/app.css --minify",
    "watch:css": "tailwindcss -i ./static/src/input.css -o ./static/css/app.css --watch"
  },
  "devDependencies": {
    "alpinejs": "^3.14.8",
    "htmx.org": "^2.0.4",
    "tailwindcss": "^3.4.0"
  }
}
